<?php
class Sns
{
	public function initialize(type,token,refresh_token,secret,expires_in,expires_at) {
    $type = type;
    $token = token;
    $refresh_token = refresh_token;
    $secret = secret;
    $expires_in = expires_in.to_i;
    $expires_at = expires_at.to_i;
    if(oauth_info.is){
    	
    }
      
  }
	
}